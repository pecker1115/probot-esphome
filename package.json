{
  "name": "probot-esphome",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "npm run build && sls deploy --stage prod",
    "dev": "npm run build && probot run ./lib/main.js",
    "logs": "sls logs --stage prod -f webhooks --tail",
    "test": "ts-mocha",
    "test:watch": "ts-mocha --watch"
  },
  "main": "./lib/main.js",
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@octokit/webhooks-types": "^5.2.0",
    "@types/debounce": "^1.2.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.8",
    "mocha": "^9.1.3",
    "prettier": "^2.5.1",
    "serverless": "^2.72.0",
    "ts-mocha": "^9.0.2",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "codeowners-utils": "^1.0.2",
    "ignore": "^5.2.0",
    "probot": "^12.2.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "arrowParens": "always"
  }
}
